@@ -, +, @@ 
 src/launch.c |    3 +++
 1 files changed, 3 insertions(+), 0 deletions(-)
--- a/src/launch.c	
+++ a/src/launch.c	
@@ -630,6 +630,9 @@ launch_appliance (guestfs_h *g)
 
     add_cmdline (g, "-nographic");
 
+    add_cmdline (g, "-vga");
+    add_cmdline (g, "cirrus");
+
     if (g->smp > 1) {
       snprintf (buf, sizeof buf, "%d", g->smp);
       add_cmdline (g, "-smp");
